# Pinned to the verified environment: these exact versions were installed
# when the shipped model artifacts were trained and the full test +
# behavior suite passed (July 2026, Python 3.14).
# XGBoost pickles are NOT portable across versions: if you bump xgboost,
# retrain (python3 initial_learn.py --no-tune) or load the version-safe
# *_uncalibrated_xgb.json / cr_predictor_model.json boosters instead.
pandas==3.0.3
numpy==2.5.1
xgboost==3.3.0
scikit-learn==1.9.0
joblib==1.5.3
streamlit==1.59.1
matplotlib==3.11.0
seaborn==0.13.2
optuna==4.9.0
plotly==6.8.0
pytest==9.1.1

# Optional — only needed for the experimental CTGAN scripts
# (gan_trial.py, maybe_master_script.py):
# ctgan>=0.10
